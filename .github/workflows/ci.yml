name: CI
on:
  push:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-node@v6
        with:
          node-version: '24'
      - uses: actions/checkout@v5
      - run: corepack enable
      - run: pnpm install
      - run: pnpm build
      - run: node -v
      - run: pnpm lint
      - run: pnpm test
